export const commentedPostImageComponent = (el) => {
    return `<div class="item__image">
            <img class="item__image--comment" src="${el.commentedPostImage}" alt="random">
        </div>`;
};
